schema_version: "1"
description: "Project coordinator — manages agent teams, delegates all implementation, drives progress autonomously"
agent_instructions: agents.md
system_prompt: system-prompt.md

skills:
  - uri: "gh://scion-frontiers/agent-team/artifact-durability"
  # agents.md → Project Sizing states the tier table is owned by
  # software-engineering-process and deliberately kept nowhere else, so the
  # coordinator cannot classify a project without it.
  - uri: "gh://scion-frontiers/agent-team/software-engineering-process"
